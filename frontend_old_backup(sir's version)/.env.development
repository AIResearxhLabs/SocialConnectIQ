# Frontend Development Environment Configuration
# This file is used by Create React App in development mode

# API Gateway Base URL - ALL requests should go through the gateway
# The API Gateway will route to the appropriate backend service
# Port 8000 = API Gateway (routes to Integration Service 8002, Backend Service 8001, Agent Service 8006)
REACT_APP_API_BASE_URL=http://localhost:8000

# Keep gateway URL for consistency (both should be 8000)
REACT_APP_GATEWAY_URL=http://localhost:8000

# Firebase Configuration (will be loaded from firebase.ts)
# Add your Firebase config here if needed

# Enable debug logging
REACT_APP_DEBUG=true
